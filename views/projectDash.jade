extends ./template.jade

block vars
  - var title = 'Project Dashboard'

block content

  
    .container
      // Main component for a primary marketing message or call to action
      .jumbotron
        h1 #{projects.projectInfo.name}
        h2 Project Info
        table(style='width:100%' border='1')
          tr
            th Project Name
            th Project Number
            th address
            th city
            th state
            th zip
            th Project Manager 
            th Email 
            th Phone
            th Project Superintendent
            th Email 
            th Phone
          tr
          td #{projects.projectInfo.name}
          td #{projects.projectInfo.number}
          td #{projects.projectInfo.address}
          td #{projects.projectInfo.city}
          td #{projects.projectInfo.state}
          td #{projects.projectInfo.zip}
          td #{projects.projectTeam.projectManager.name}
          td #{projects.projectTeam.projectManager.email}
          td #{projects.projectTeam.projectManager.phone}
          td #{projects.projectTeam.projectSuperindendent.name}
          td #{projects.projectTeam.projectSuperindendent.email}
          td #{projects.projectTeam.projectSuperindendent.phone} 
          
    // /container
    //
      Bootstrap core JavaScript
      ==================================================
    // Placed at the end of the document so the pages load faster
    script(src='https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js')
    script.
      window.jQuery || document.write('<script src="/js/jquery.min.js"><\\/script>')
    script(src='/js/bootstrap.min.js')
    // IE10 viewport hack for Surface/desktop Windows 8 bug
    script(src='/js/ie10-viewport-bug-workaround.js')
